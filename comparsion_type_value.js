var firstVal = 5;
var secondVal = "5";
// compare test values and types
if (firstVal === secondVal) {
  'use strict';
    console.log("They are the same");
} else {
    console.log("They are NOT the same");
}